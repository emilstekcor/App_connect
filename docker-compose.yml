version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - .env.example
    volumes:
      - ./server/uploads:/app/uploads
  client:
    build: ./client
    ports:
      - "5173:5173"
    env_file:
      - .env.example
